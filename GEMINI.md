Read `AGENTS.md` at the repo root before doing anything — it is the canonical agent instruction file for this project (conventions, architecture map, sharp edges). Then read `TODO.md`: each item is a self-contained spec with decisions already made; start with item 0.

Do not build TODO item 9 (joins) without a design session with the owner.
