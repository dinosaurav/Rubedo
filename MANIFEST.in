# Include the web/ source so sdist installs can build the UI
recursive-include web *.ts *.tsx *.json *.html *.css *.svg *.js

# Include built web assets (generated by setup.py's build hook)
recursive-include src/rubedo/web_static *
