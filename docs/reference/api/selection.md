# Selection, trace & invalidation

Querying what the store holds and surgically invalidating it: the
`Selection` query language, lineage `trace()`, and `invalidate()`.

::: rubedo.selection.Selection

::: rubedo.trace.trace

::: rubedo.trace.TraceResult

::: rubedo.trace.TraceNode

::: rubedo.invalidation.invalidate
