# Steps

Declaring the units of work: the `@step` decorator, the `StepSpec` it
mints, and the `Filtered` verdict a step returns to decline an item.

::: rubedo.spec.step

::: rubedo.spec.StepSpec

::: rubedo.models.Filtered
