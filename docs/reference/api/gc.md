# GC & storage

Retention garbage collection and disk-usage reporting. These live in
their own submodules (`rubedo.gc`, `rubedo.du`) rather than the
top-level package.

::: rubedo.gc.gc

::: rubedo.gc.GcReport

::: rubedo.du.storage_report

::: rubedo.du.StorageReport
