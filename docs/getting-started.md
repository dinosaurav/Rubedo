# First run

This URL used to be its own page. The first run now lives in two places:

1. **[Docs home](index.md)** — identity, the two-function snippet, what
   the code is doing.
2. **[Tutorial](tutorial.md)** — install, copy-paste a classifier, what
   `plan()` prints, run-it-twice, and the `.rubedo/` working-directory
   rule.

Start at the [tutorial](tutorial.md) if you want to type along.
